version: '2.1'

services:
    inkyshot:
        build: ./inkyshot
        privileged: true
        labels:
            io.balena.features.balena-api: '1'
    wifi-connect:
        build: ./wifi-connect
        restart: always
        network_mode: host
        privileged: true
        labels:
            io.balena.features.dbus: '1'
            io.balena.features.firmware: '1'